[project]
name = "iot-sensors-simulation"
version = "0.1.0"
description = "Sumulador de sensores IoT para broker MQTT (mosquitto)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "paho-mqtt>=2.1.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
